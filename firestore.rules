rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Open access for the demo period - Ensure security rules are hardened for production later
      allow read, write: if true;
    }
  }
}