import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.egx.todolist',
  appName: 'Todo List EGX',
  webDir: 'dist/angular-task-challenge/browser'
};

export default config;
